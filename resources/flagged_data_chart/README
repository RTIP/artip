The scripts requires following pip modules:
---------------------------------------------
$ pip install pygal
$ pip install tinydb

The chart.py script plots the flagged data percentage at different stages of the pipeline.

Before capturing the flagged data percentage snapshot after a particular stage of pipeline, check the charting_config.
The charting_config contains inputs to be given to the casac get_data.

To capture the flagged data percentage at different stages
$ python chart.py 'path_to_ms.ms' stage1
$ python chart.py 'path_to_ms.ms' stage2

To plot the flagged data percentage chart
$ python chart.py 'path_to_ms.ms' plot