#!/usr/bin/env python

import sys
import subprocess
#from mpi4py import MPI
#from mpi4casa.MPIEnvironment import MPIEnvironment

#print "MPI Enabled = {0}".format(MPIEnvironment.is_mpi_enabled)

#MPIEnvironment.is_mpi_enabled=False

#print "MPI Enabled = {0}".format(MPIEnvironment.is_mpi_enabled)
print "This worked!"
#command = "casa --nogui --logfile {0} -c \"exit()\"".format(sys.argv[1])
#subprocess.Popen(command, stdin=subprocess.PIPE, stdout=file(sys.argv[2], 'a+'), shell=True).wait()
